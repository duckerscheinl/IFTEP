# IFTEP — Python dependencies
#
# Install into a fresh environment with:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# PyTreeNet requires Python >= 3.8.10.

# --- numerics ---
numpy>=1.24.3
scipy>=1.10.1
h5py>=3.10.0
tqdm>=4.65.0

# --- plotting ---
matplotlib>=3.7

# --- notebook ---
jupyterlab>=4.0
ipykernel>=6.24.0

# --- tree tensor networks -------------------------------------------------
# Tracks the current main branch of the upstream repository:
pytreenet @ git+https://github.com/Drachier/PyTreeNet.git